import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io..starter',
  appName: 'test',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
